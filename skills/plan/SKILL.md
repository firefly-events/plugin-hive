---
name: plan
description: |
  Decompose a requirement into an epic with dependency-tracked stories. Use when
  the user says "plan this feature", "break this into stories", "decompose this",
  "create an epic", or provides a feature description that needs structured
  breakdown before implementation. Also triggers on "hive plan" or any request
  to turn a requirement into implementable work items.
---

# Hive Plan

Decompose a requirement into an epic with dependency-tracked stories.

**Input:** `$ARGUMENTS` contains the requirement or feature description.

**Instructions:** Read the full Hive skill at `skills/hive/ORCHESTRATOR.md` and follow the **Plan Command** section. All shared resources (agents, references, workflows) are in the `skills/hive/` directory.
