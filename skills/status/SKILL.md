---
name: status
description: |
  Check the status of active Hive workflow epics. Shows per-story progress,
  current phase, dependency graph, and episode summaries. Use when the user
  says "what's the status", "hive status", "show progress", "how far along",
  or wants to see the state of running or planned workflows.
---

# Hive Status

Report the status of active workflow epics.

**Input:** `$ARGUMENTS` optionally contains an epic ID to filter to a single epic.

**Instructions:** Read the full Hive skill at `skills/hive/ORCHESTRATOR.md` and follow the **Status Command** section. Episode files are in `state/episodes/` and epic files in `state/epics/`.
