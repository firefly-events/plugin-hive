# State Dir Resolver Brief Writing Insight

For relocation epics, separating `.pHive` hits by consumption surface is more useful than listing every path. The raw count is large enough that a design discussion needs clustered seams: code runtime, tests, and prose-that-executes each require different resolver mechanisms and different exception rules.
