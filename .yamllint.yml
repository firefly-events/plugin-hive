extends: relaxed
rules:
  line-length: disable
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false
  key-duplicates: disable  # cycle-state files use intentional duplicate keys (status/phase) to track progression
