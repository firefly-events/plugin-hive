# KG density verification — opt-in, OFF by default.
#
# Enable by setting the repository variable KG_DENSITY_CHECK_ENABLED to 'true'
# (Settings -> Secrets and variables -> Actions -> Variables). Without it the
# scheduled run is skipped; workflow_dispatch always works for manual checks.
name: kg-density-check

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 1 * *' # monthly, 1st at 06:00 UTC — gated by KG_DENSITY_CHECK_ENABLED

permissions:
  contents: write
  pull-requests: write

jobs:
  verify:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || vars.KG_DENSITY_CHECK_ENABLED == 'true'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run density verification
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "kg-density-bot"
          git config user.email "kg-density-bot@users.noreply.github.com"
          # kg.sqlite lives outside the repo (~/.claude/hive/kg.sqlite); in CI it
          # must be provided (e.g. restored from an artifact or runner state).
          # Missing db exits 1 with a clear message — the scheduled run then
          # simply reports "thresholds not verifiable" instead of failing silently.
          node scripts/kg-density-verification.mjs
