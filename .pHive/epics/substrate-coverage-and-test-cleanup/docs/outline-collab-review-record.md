# Outline collab review record — substrate-coverage-and-test-cleanup

**Phase:** B3 (outline collab review)
**Run ID:** wf_088c4cf3-f0c
**Generated:** 2026-06-05T12:40:00Z
**Reviewers:** researcher, tpm, architect
**Document under review:** structured-outline.md (750 lines, 7 phases, 19 stories)

All three returned `approve-with-escalation`. This is the FINAL collab review before user sign-off.

---

## Researcher review

REVIEW: researcher
VERDICT: approve-with-escalation
COMMENTS:
  - Part 4 file manifest omits test file paths entirely despite Part 3 verification plan calling for ~14 vitest assertions (mode-resolver 5-tier test, execute-dispatch snapshot test, assertWorktreeIsolation test, markNeedsRework adapter contract + both adapter tests, test-mode-cc-workflows resolution test, scenario-replay loading test, design/review/design-review resolution tests, scope_drift preservation test). For TDD-methodology story t-2 specifically, the contract test path should be named in the manifest so Phase C story authoring has a concrete target. Repo convention (likely `hive/lib/<module>/test/*.test.ts` or similar) is implied but not stated.
  - All spot-checked file:line citations match research-brief verbatim: execute-dispatch:46-101, task-tracking-dispatch:205-282 + 285-288, multica/index.ts:335-348 + 20-26, github/index.ts:291-309 + 296-302, step-06-triage:1-129 + 111-114, test-sentinel:1-125, design/SKILL.md:55-64, design-review.workflow.yaml:8-81. No fabricated layer/file claims found.
  - Phase 0 audit recovery has three concrete recovery paths: (a) re-walk audit directory + sibling locations + copy into `.pHive/audits/post-run/`, (b) run cc-workflows smoke locally + produce fresh audit under ISO timestamp, (c) write `audit-recovery-decision.md` with explicit accept-risk rationale + update design-discussion §5. Not generic "find or rewrite" — each branch has a named output.
  - Phase 1 foundation file manifest covers all 3 sub-stories: s-2 creates `hive/lib/mode-resolver.mjs` + refactors `execute-dispatch/SKILL.md` Step 0; s-4 creates `hive/lib/cc-workflows-preconditions.mjs` + back-fits both `plan-mode-cc-workflows` and `execute-mode-cc-workflows`; s-3 creates `hive/scripts/lint-cc-workflows-no-codex.mjs` + wires into `package.json`. Complete.
  - Phase 2 t-2 file manifest covers BOTH adapter implementations (multica/index.ts:335-348, github/index.ts:291-309), ABI surface (task-tracking-dispatch/index.ts:205-282 + capability advertisement at 285-288), test-sentinel emit (step-06-triage.md line 16 classification + summary block 111-114 extension), AND prose-only test-sentinel persona update (correctly scoped — no executable change, preserving step-vs-persona discipline). Tests omitted from manifest as noted above.
  - Phase 3 d-5 handoff payload locked at PNG + .f0 + bundled constraint doc per user Q9 resolution (lines 209-213); baked into d-5 AC at line 213 ("Bake into d-5 AC") and verification matrix at line 358; toggle-off behavior preserved (PNG + .f0 always ship, constraint doc field empty/absent when toggle off).
  - Risk registry correctly carries Constraint 5 single-citation acceptance through Risk 2 (Slice 0); Constraint 4 no-Codex enforcement through Risk 8 (s-3 widened lint); Constraint 6 worktree-isolation through Risk 6 (s-4 helper + lint sub-step); Constraint 1 ABI mismatch through Risk 1 (TDD methodology); Constraint 3 Multica state restriction through Multica adapter implementation note (`in_review` + label). Each constraint maps to a risk row with named owner.
ESCALATION_FLAGS:
  - Add named test file paths to Part 4 manifest for at minimum t-2's TDD adapter contract test (the only TDD-methodology story in the epic) so Phase C story authoring has a concrete TDD-first target rather than inferring location from repo convention. Recommended at the s-2 (mode-resolver) and s-4 (preconditions) level too since both have named vitest assertions in Part 3.

---

## TPM review

REVIEW: tpm
VERDICT: approve-with-escalation
COMMENTS:
  - **Phase 1 within-slice commit order — ENCODED correctly.** The s-2→s-4→s-3 order is locked in Part 1 §"Key decisions" #5, Part 2 Phase 1 header "(within-slice commit order LOCKED)", the per-story narrative explicitly justifies why reverse fails ("lint to fail on missing imports"), Part 6 dependency map re-states it, and Decision Point 4 surfaces it for user re-affirmation. This is encoded, not just mentioned. Pass.
  - **Phase 0 spike acceptance signal — TWO of three branches have concrete "done" criteria; branch (c) is softer.** Branch (a) "ls .pHive/audits/post-run/ shows the recovered audit" and branch (b) "fresh audit under ISO timestamp" are file-system observable. Branch (c) requires `audit-recovery-decision.md` to exist with "accept-risk rationale" — this is a doc-creation gate, not a content-quality gate. An implementer could write a 2-line decision doc and pass acceptance. Recommend the AC for branch (c) specify required sections (e.g., "what makes plan-mode-validation audit sufficient corroboration; what risk we accept; what runtime carrier we rely on"). Minor — flag, not block.
  - **Phase 2-5 foundation dependency — EXPLICIT per phase.** Each of Phase 2/3/4/5 carries "Depends on: Slice 1 (foundation)" header line, AND each Phase header states "Independent of Slices [other parallel-eligible numbers]". Phase 3 also calls out d-1 needs the orchestration template citation at `design-review.workflow.yaml:8-81` — that's pre-existing, not a Slice 4 dep. No hand-waving. Pass.
  - **Phase 6 sizing — REALISTIC for 1 story.** Slice 6 is described as "pure summary work; no upstream authoring lives in this slice." The 6×3 matrix has 18 cells; each cell is either a path-paste or an N/A rationale. Verification is "every cited path resolves" — a grep+ls walk. E5 elicitation explicitly notes the matrix should be "mechanical reading from the artifacts in place" and proposes a Slice 5-close dry-run to surface surprises early. Sizing is honest. Pass.
  - **Elicitation Part 7 — GENUINE stress tests, not rephrases.** E1 tests the parallel-runtime assumption (V-plan §5 dep, not previously challenged at this depth). E2 probes whether the TDD contract symmetry actually delivers semantic symmetry downstream (genuinely adversarial — answer is "downstream notifications ARE adapter-specific and that's intentional"). E3 tests review-stall blast radius on the locked commit order. E4 questions whether 3-AC-subsections actually prevent the drift the architect claims it prevents (confidence MEDIUM is honest). E6 surfaces the transitive-helper-import escape (becomes Decision Point 3). These are real stress tests, not re-asked Phase A questions. Pass.
  - **Decision points Part 8 — ONE potential miss for story authoring.** Decision 6 (H3 `loadScenario` schema verification BEFORE t-1 AC freeze) is correctly surfaced. But there is NO decision point on whether the **3-persona handoff payload (Q9: PNG + .f0 + constraint doc)** is locked vs. negotiable during d-5 implementation. E7 calls this out as MEDIUM confidence with reversible follow-up additions, but the user signed off on the shape — if downstream consumers later need version metadata or ticket ID, will d-5 be allowed to expand, or is the payload contract frozen? Story author needs to know.
ESCALATION_FLAGS:
  - **Branch (c) acceptance criterion for Slice 0 is underspecified.** The 3-branch resolution is sound, but branch (c)'s "decision doc exists with accept-risk rationale" needs minimum-content AC (what risk, what corroboration replaces the missing citation, what runtime carrier substitutes). Without this, an implementer can produce a thin decision doc and pass Phase 0 acceptance while leaving Constraint 5's gate-ownership invariant genuinely under-grounded. Recommend adding 3-4 required sections to the decision doc AC before story authoring begins.
  - **Wireframe handoff payload contract — locked vs. extensible posture needed for d-5.** User-locked PNG + .f0 + constraint doc shape (Decision 7-adjacent, Q9). E7 acknowledges downstream consumers may need additional fields (scenario-mapping, version metadata, ticket ID) as reversible follow-ups. Story author needs an explicit posture statement: is d-5's payload "the contract, additions require a follow-up epic" OR "the minimum payload, d-5 is free to add fields if a downstream consumer surfaces a need during the manual exercise step (E7 recommends)"? This affects how d-5 AC is written and how downstream consumers (design-review in Slice 4) test against the handoff.

---

## Architect review

REVIEW: architect
VERDICT: approve-with-escalation
COMMENTS:
  - **Phase 1 s-2 helper signature consistency:** Outline locks `{decision, sources}` at line 94 and reiterates `{mode_decision, sources}` at consumer routers (t-3 line 159, d-2 line 199, dr-1, r-1). However, **the 6 consumer dispatch sites are NOT enumerated by name** in s-2's story body. They surface scattered across Phases 2-5 (test-dispatch, design-dispatch, design-review-dispatch, review-dispatch + the 2 existing execute-dispatch/plan-dispatch). Story authoring will need to derive this consumer list from V-plan cross-references; not a blocker but worth surfacing as a Phase C input note. Also note minor naming drift: helper returns `{decision, sources}` (s-2) but routers return `{mode_decision, sources}` (t-3, d-2) — sound architecturally (router wraps helper output) but should be called out explicitly so implementers don't lint-fail on the name mismatch.
  - **Phase 1 s-4 back-fit:** Explicit. Line 104 ("Back-fit: add helper import + Step 0 helper call to BOTH existing cc-workflows atoms (plan-mode-cc-workflows, execute-mode-cc-workflows)") + manifest lines 455-456 cite both files with research-brief refs. Clean.
  - **Phase 2 t-2 ABI shape:** Mostly explicit but **one structural gap**. Outline names `markNeedsRework({id, reason})` method, per-adapter mappings (line 146 Multica `updateStory({status:'in_review'}) + label`; line 147 GitHub `reopen + label`), and capability flag `supports_needs_rework` (line 147, 464). However, the **return shape of `markNeedsRework` is not specified** — neither in the t-2 story body nor in Part 4 manifest. Risk 1 detail (line 518) implies an external observable (terminal state + label) but no return type. Story authoring needs `Promise<{id, status, labels}>` or equivalent to write a contract test. Recommend adding one sentence to t-2 specifying return shape before story YAML.
  - **Phase 3 d-1 AC subsections:** Explicitly required as 3 named subsections — line 34 ("3 named subsections"), line 174-195 enumerate them by name (Phase A structural insert / Pattern B toggle semantics / 3-persona handoff payload). Risk 11 reinforces (line 508). Clean.
  - **Phase 3 d-2 design-dispatch mirror reference:** **GAP.** d-2 at lines 197-199 is terse — consumes `mode-resolver.mjs` from Slice 1 and returns `{mode_decision, sources}`. **No explicit mirror citation** to either `plan-dispatch` or `execute-dispatch`. By contrast, d-1 cites `design-review.workflow.yaml:8-81` as the architectural anchor (line 181); d-3 cites `execute-mode-multica` (line 203); d-4 cites `plan-mode-cc-workflows` (line 207). d-2 (and by parity dr-1, r-1, plus t-3's test-dispatch portion at line 159) lack the mirror reference. Story authoring would benefit from explicit "mirror `execute-dispatch/SKILL.md` structure" anchor to prevent re-invention.
  - **Phase 4 dr-2 design-review-mode-multica dispatch granularity:** Outline says (line 233) "Preserves the 4-step workflow.yaml model with 4 `agent()` calls (substrate parity with workflow.yaml structure)". **This is 4 agent() calls within a single Multica run, NOT per-persona dispatch with 3 separate episode markers.** Contrast with d-3 design-mode-multica (line 203) which IS per-persona ("per-persona dispatch within a team-cell; episode markers per persona"). The asymmetry between d-3 (per-persona episodes) and dr-2 (single workflow with 4 internal agent() calls) is intentional per Q11 resolution but may surprise implementers. **Architectural soundness: OK** because design-review's workflow shape is itself the canonical anchor; dr-2 is replicating that shape in Multica. But this distinction should be explicit in the d-3 vs dr-2 contrast, otherwise story authoring may incorrectly apply per-persona to dr-2 by symmetry.
  - **Phase 5 r-1/r-2/r-3 solo reviewer:** Explicitly confirmed. Line 245 ("solo reviewer (no panel-mode expansion in this epic)"), line 253 ("Preserve solo reviewer pattern (lines 39-49)"), Decision Point 2 (line 710-715). Clean.
  - **Phase 6 dispatch-parity.md matrix:** Outline at lines 280-282 specifies rows (`/plan`, `/execute`, `/test`, `/design`, `/design-review`, `/review` — 6 rows) and cols (`multica`, `cc-workflows`, `inline` or `github` — 3 cols). **The 6×3 = 18 cells are NOT individually enumerated** in s-1's story body, nor is per-cell state pre-declared. E5 elicitation (line 647-659) acknowledges this gap explicitly: "If a missing cell surfaces, the response is to mark N/A with an honest rationale." Architecturally this is sound (matrix is mechanical reading from prior slices) but story authoring should pre-stamp each cell as `expect: <path>` or `expect: N/A` BEFORE Slice 6 to avoid surprise. Recommend a tabular pre-declaration in s-1 story AC.
ESCALATION_FLAGS:
  - **t-2 return shape underspecified** — `markNeedsRework({id, reason})` ABI lacks return-type contract. Add one sentence to Phase 2 t-2 specifying return shape (e.g., `Promise<{id, status, labels[]}>`) before story YAML authoring. Otherwise the TDD contract test in t-2's locked ordering (step 1) has nothing to assert against.
  - **d-2 / dr-1 / r-1 / test-dispatch-portion-of-t-3 lack mirror-reference anchor** — d-1, d-3, d-4 all cite a concrete architectural anchor (design-review.workflow.yaml, execute-mode-multica, plan-mode-cc-workflows respectively). The 4 dispatch routers (one per substrate slice) do NOT cite `execute-dispatch/SKILL.md` as their mirror despite being structurally identical. Risk: story authoring re-invents router shape per-slice. Recommend adding "mirror `execute-dispatch/SKILL.md` structure (Step 0/1/2)" to each dispatch story.
