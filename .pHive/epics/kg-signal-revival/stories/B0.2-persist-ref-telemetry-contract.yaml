id: B0.2-persist-ref-telemetry-contract
epic: kg-signal-revival
title: Persist + reference + telemetry contracts document
status: pending
complexity: low
methodology: classic
depends_on: []
wave: B0

description: |
  Author the B0.2 contract document. Three contracts in one file:
  (a) ChromaDB "decisions" collection schema + metadata fields (consumed by S3).
  (b) Registry row shape (consumed by S4).
  (c) Telemetry envelope: hit-rate gauge shape, miss-reason taxonomy buckets,
      per-predicate write counter names (consumed by S5).

  Defines field names + types BEFORE S3/S4/S5 implementations write them. Prevents
  S5 telemetry counters from referencing schema fields S3 hasn't agreed to.

acceptance_criteria:
  - "Doc exists at .pHive/epics/kg-signal-revival/docs/registry-chroma-telemetry-contract.md"
  - "ChromaDB decisions collection schema: id field, document field, metadata fields enumerated"
  - "Registry row shape matches existing projects.yaml entries + adds any new fields"
  - "Telemetry: 4 counter names + 5 miss-reason buckets enumerated"

steps:
  - id: research
    description: Inspect existing projects.yaml row shape + chromadb-wrapper.js index() signature + metric_registry.py existing counter pattern.
    agent: researcher
  - id: implement
    description: Write registry-chroma-telemetry-contract.md with 3 sub-sections.
    agent: developer
    depends_on: [research]
  - id: test
    description: Cross-check each contract field against the consumer it serves; flag mismatches.
    agent: tester
    depends_on: [implement]
  - id: review
    description: Validate field types + completeness.
    agent: reviewer
    depends_on: [test]
  - id: integrate
    description: Commit.
    agent: developer
    depends_on: [review]

context:
  codebase: /Users/don/Documents/plugin-hive
  key_files:
    - path: hive/lib/chromadb-wrapper.js
      purpose: index() signature defines metadata field expectations
    - path: ~/.claude/hive/projects.yaml
      purpose: Existing registry row shape
    - path: skills/hive/skills/meta-optimize/metric_registry.py
      purpose: Existing counter registration pattern

cross_cutting:
  - concern: documentation
    action: "Contract doc is the deliverable."
  - concern: metrics
    applies: false
    justification: "Docs sliver."

metric:
  applies: false
  justification: "Planning artifact."
