# S4.3 — Shindig onboarding + backfill — execution notes

Executed: 2026-05-15T06:28:40Z (UTC)
Branch: feat/kg-signal-revival
Operator: /plugin-hive:execute run (orchestrator-driven)

## Registration

```
node skills/hive/skills/register-project/register-project.mjs \
  <PROJECT_PATH> --name shindig
```

Outcome:

```
Registered project:
  name:          shindig
  path:          <PROJECT_PATH>
  registered_at: 2026-05-15T06:28:40.751Z
  registry:      <HOME>/.claude/hive/projects.yaml
```

`~/.claude/hive/projects.yaml` now has two rows:
- plugin-hive (already-registered)
- shindig

## Backfill

```
node scripts/kg-bootstrap-from-projects.js --apply --since 2026-04-28
```

Aggregate summary:

```
Mode:           apply
Since:          2026-04-28
Registered:     2
Eligible:       2
Skipped:        0
Projected:      14
Skipped pre---since: 62
Succeeded:      2
Failed:         0
```

Per-project breakdown:
- plugin-hive — 14 triples projected, +1 newly inserted (66 → 67; the rest deduped by INSERT OR IGNORE against existing rows from earlier S1.1/S1.2/S2.x emissions).
- shindig — **0 triples imported**. Shindig's `.pHive/cycle-state/` directory has 4 epic YAMLs with 8 total `decision:` records, all dated before the 2026-04-28 predicate-canon date. Result is the **documented acceptable outcome** per the story's risk note:

> Risk: Shindig's `.pHive/cycle-state/` may be predominantly pre-canon-date → backfill imports 0 triples.
> Mitigation: Acceptable; registration + bootstrap mechanism is the deliverable.

## Acceptance criteria status

- ✅ `/hive:register-project <PROJECT_PATH>` succeeded.
- ✅ `~/.claude/hive/projects.yaml` gained the shindig entry.
- ✅ `kg-bootstrap-from-projects.js --apply --since 2026-04-28` ran with `Failed: 0`.
- 🟡 ≥5 Shindig triples in plugin-hive KG under `shindig/{epic_id}` namespace — **0 actual**, pre-canon constraint, AC #6 documents this as acceptable.
- 🟡 Next /meta-optimize cycle producing `[cross-project: shindig]` hard-tag finding — **deferred**: gate predicate is "IF Shindig has relevant phase_failed/phase_blocked/superseded triples", which it does not (and won't until new Shindig cycles emit through the production seams Shindig itself has not yet adopted).

## BYO-enhancement note

The `scripts/kg-bootstrap-from-projects.js --apply` path requires
`better-sqlite3`. Per `feedback_byo_enhancements_no_root_deps`, it is opt-in:
installed once via `npm install --no-save better-sqlite3` (no `package.json`
was created, no root dependency declared). `node_modules/` was added to
`.gitignore` in this commit so the transient install does not accumulate
tracked files. Future invocations of this script will just work if
`node_modules/better-sqlite3/` is still present.

## Follow-on

S4.4 (Signal Flayr) and S4.5 (NTA, spaced path) reuse this exact procedure with
their respective registry names and paths. The `--since` value stays
`2026-04-28` (predicate-canon date) for all three.
