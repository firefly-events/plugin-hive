id: stmc-2-append-line
epic: smoke-test-execute-multica-codex
title: Append a known line to smoke-marker.txt
status: blocked
complexity: low
methodology: classic
depends_on: [stmc-1-create-marker]

description: |
  Append a second line to .pHive/smoke/smoke-marker.txt:
  "smoke-test:appended:<ISO 8601 timestamp>". File should end with a
  newline.

acceptance_criteria:
  - "File .pHive/smoke/smoke-marker.txt has exactly two lines"
  - "First line is unchanged from stmc-1"
  - "Second line starts with 'smoke-test:appended:'"
  - "Second line ends with a valid ISO 8601 UTC timestamp"

steps:
  - id: implement
    description: |
      Append a single line to .pHive/smoke/smoke-marker.txt of the form
      "smoke-test:appended:<UTC ISO 8601 timestamp>\n". Do NOT modify the
      existing first line.
    agent: developer

context:
  files_to_modify:
    - file: .pHive/smoke/smoke-marker.txt
      change: append a single timestamped line
