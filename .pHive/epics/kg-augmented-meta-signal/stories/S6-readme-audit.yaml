# Synthesized 2026-05-11 during M-07 retro from episode evidence at
# .pHive/episodes/kg-augmented-meta-signal/S6-readme-audit/.
# Original story body was never committed; this YAML reconstructs the
# story from the matching episode implement.yaml + the drift-checklist
# at .pHive/epics/kg-augmented-meta-signal/docs/readme-drift-checklist.md.
# Story-level `backfilled: true` flags the synthesis.

id: S6-readme-audit
epic: kg-augmented-meta-signal
title: README + CHANGELOG audit — 1.1.3 drift cleanup and 1.1.4 kg-signal coverage
status: completed
backfilled: true
complexity: small
methodology: classic
depends_on: [S5-fixture-test]
wave: S6
action_id: S6

description: |
  Update README.md and CHANGELOG.md to reflect the 1.1.4 release adding the
  kg-augmented-meta-signal feed (step-02c). Catch any drift from 1.1.3
  that wasn't updated when 1.1.3 shipped. Produce a drift-checklist doc at
  .pHive/epics/kg-augmented-meta-signal/docs/readme-drift-checklist.md
  enumerating every README / CHANGELOG section touched so the next release
  audit has a clean starting point.

  This is documentation-only — no code changes, no schema changes.

acceptance_criteria:
  - "README.md mentions step-02c kg-signal feed in the workflow section"
  - "CHANGELOG.md has a 1.1.4 entry citing the kg-augmented-meta-signal epic"
  - "drift-checklist.md exists at .pHive/epics/kg-augmented-meta-signal/docs/ enumerating every section touched"
  - "No functional code changes (docs-only PR)"

steps:
  - id: research
    description: Diff README.md and CHANGELOG.md against the actual 1.1.3 → 1.1.4 feature set; list drift candidates.
    agent: researcher
  - id: implement
    description: Apply doc edits; author the drift-checklist as a living audit artifact.
    agent: technical-writer
    depends_on: [research]
  - id: test
    description: Grep README + CHANGELOG for the kg-signal / step-02c references; confirm 1.1.4 changelog entry exists.
    agent: tester
    depends_on: [implement]
  - id: review
    description: Confirm no code changes in diff; confirm drift-checklist is structured (per-section table).
    agent: reviewer
    depends_on: [test]
  - id: integrate
    description: Commit on feat/kg-augmented-meta-signal.
    agent: developer
    depends_on: [review]

context:
  codebase: /Users/don/Documents/plugin-hive
  key_files:
    - path: README.md
      purpose: Primary doc — must reference step-02c kg-signal feed
    - path: CHANGELOG.md
      purpose: Must gain 1.1.4 entry
    - path: .pHive/epics/kg-augmented-meta-signal/docs/readme-drift-checklist.md
      purpose: NEW — release-audit artifact

metric:
  applies: true
  backfilled: true
  name: kg_augmented.readme_changelog_drift_closed
  direction: up
  unit: count
  baseline: 0
  target: 3
  window: "story-integrate"
  source:
    kind: manual
    ref: "(test -f .pHive/epics/kg-augmented-meta-signal/docs/readme-drift-checklist.md) + (grep -c 'kg-signal\\|step-02c' README.md) + (grep -c '1\\.1\\.4' CHANGELOG.md) — three independent presence-checks, target=3 means all three pass — falsifiable at planning time using only file presence + grep"
  verify_at: "story-integrate"
  owner: developer
