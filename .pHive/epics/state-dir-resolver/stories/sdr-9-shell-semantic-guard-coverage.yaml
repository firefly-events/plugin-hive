id: sdr-9-shell-semantic-guard-coverage
epic: state-dir-resolver
title: Make shell semantic guard relocated-state aware
status: complete
complexity: low
methodology: classic
depends_on: [sdr-1-resolver-contract-conformance-fixture]
tracker_id: "PLU-256"
tracker_uuid: "ae7cedce-8686-4e60-a75d-00cc7eea709a"
tracker_url: "http://localhost:8080/issues/ae7cedce-8686-4e60-a75d-00cc7eea709a"
parallel_allowed: true
parallel_rationale: bounded-slice
description: |
  Update the shell misuse guard so story-path detection recognizes the resolved
  state-dir basename in addition to default `.pHive` and legacy `state`. This is
  a small shell-only coverage slice that uses the sdr-1 resolver contract.
acceptance_criteria:
  - "Given default `.pHive`, when misuse occurs under a story path, then `hooks/check-agent-misuse.sh` still flags it."
  - "Given a custom `paths.state_dir`, when misuse occurs under the resolved state-dir story path, then the guard flags it."
  - "Given either default or custom state dir, when access is legitimate, then the guard does not flag it."
  - "Given state-dir configuration is unset, when the guard runs, then default `.pHive` behavior remains unchanged."
metric:
  applies: false
  justification: "This story changes a shell semantic guard and is verified by binary shell tests for default, custom, and legitimate paths rather than by metrics."
steps:
  - id: implement
    description: Source `hooks/common.sh` as needed and update the story-path regex or matching logic to accept the resolved state-dir basename.
    agent: developer
  - id: test
    description: Add shell tests for default misuse, custom-state misuse, and legitimate access in both configurations.
    agent: tester
    depends_on: [implement]
  - id: review
    description: Verify the shell change is isolated and does not weaken misuse detection.
    agent: reviewer
    depends_on: [test]
  - id: integrate
    description: Commit the semantic guard slice to `feat/state-dir-resolver`.
    agent: developer
    depends_on: [review]
context:
  codebase: /Users/don/Documents/plugin-hive
  key_files:
    - path: hooks/common.sh
      purpose: Source of `_resolve_state_dir()` for shell-side resolved state-dir detection.
    - path: hooks/check-agent-misuse.sh
      purpose: Semantic guard whose story-path regex must become relocated-state aware.
files_to_modify:
  - file: hooks/check-agent-misuse.sh
    change: Update story-path detection to consult the resolver and accept the resolved state-dir basename.
  - file: tests
    change: Add shell guard tests for default, custom, and legitimate access cases.
cross_cutting:
  - concern: state-dir-relocation
    action: "Extend shell guard coverage to relocated state dirs while preserving default `.pHive` behavior."
references:
  - path: .pHive/epics/state-dir-resolver/docs/design-decisions.md
    relevant_excerpt: "Shell semantic-guard coverage is an implied story after the resolver contract."
  - path: .pHive/epics/state-dir-resolver/docs/vertical-plan.md
    relevant_excerpt: "Step 9 makes `hooks/check-agent-misuse.sh` recognize the resolved `<state_dir>/<story-id>` shape."
