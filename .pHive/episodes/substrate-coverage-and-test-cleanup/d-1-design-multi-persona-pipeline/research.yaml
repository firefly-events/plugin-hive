episode_marker:
  story_id: d-1-design-multi-persona-pipeline
  epic: substrate-coverage-and-test-cleanup
  step: research
  agent: researcher
  status: complete
  timestamp: "2026-06-08T00:00:00Z"

insight_capture:
  personas:
    - researcher
  count: 1
  memory_path: hive/agents/memories/researcher/d-1-phase-a-insert-is-structural-not-extension.md

findings:

  FILES_EXAMINED:
    - path: skills/design/SKILL.md:1-157
      relevance: >
        Full current shape of /design skill. Sections:
        Skill Preamble (lines 23-31): kickoff gate override, standalone mode.
        Gate Check (lines 33-35): no blocking gate, warn-only.
        Process (lines 37-157): steps 1-7.
          Step 1 (lines 39-47): parse brief, resolve topic slug, rendition count.
          Step 2 (lines 49-53): load brand context.
          Step 3 (lines 55-64): load ui-designer persona and dispatch — ONLY dispatch step. INSERT TARGET.
          Step 4 (lines 66-71): run wireframe-protocol touchpoints.
          Step 5 (lines 73-85): write wireframe artifacts.
          Step 6 (lines 87-104): emit design-review handoff record.
          Step 7 (lines 106-125): report output.
        Standalone vs delegated (lines 128-133): two invocation modes.
        What /design is NOT (lines 135-140): explicitly states it does NOT run accessibility-specialist or animations-specialist.
        Atomic-skill invariants (lines 142-147): top-level, standalone, no prior state.
        See also / references (lines 149-157).
      insert_target: "Phase A goes ABOVE step 3 (lines 55-64) as a new named section.
        Phase 0 goes ABOVE Phase A (before step 1 currently, inserted as new Phase 0)."

    - path: hive/workflows/design-review.workflow.yaml:1-81
      relevance: >
        4-step orchestration template that Phase A MUST mirror. Step definitions captured:
          Step 1 — id: accessibility-critique (lines 7-25)
            agent: accessibility-specialist
            task: review artifacts for accessibility issues + WCAG 2.1 AA
            depends_on: [] (no prereqs)
            inputs: design_artifacts (source: context, context_key: design_artifacts)
            outputs: accessibility_critique (type: string)
            optional: true, timeout_ms: 600000
          Step 2 — id: animations-critique (lines 27-52)
            agent: animations-specialist
            task: review artifacts for animation/motion quality; reference accessibility critique
            depends_on: [accessibility-critique]
            inputs: design_artifacts + accessibility_critique (source: step_output, optional)
            outputs: animations_critique (type: string)
            optional: true, timeout_ms: 600000
          Step 3 — id: design-critique (lines 54-77)
            agent: ui-designer
            step_file: hive/references/ui-prompts/design-review-design-critique.md
            depends_on: [animations-critique]
            inputs: design_artifacts + accessibility_critique (optional) + animations_critique (optional)
            outputs: design_critique (type: string)
            optional: false, timeout_ms: 600000
          Step 4 — id: synthesis (lines 79-81+)
            agent: ui-designer
            step_file: hive/references/ui-prompts/design-review-synthesis.md
            depends_on: [accessibility-critique, animations-critique, design-critique]
        Serial chain: accessibility → animations → ui-designer-critique → ui-designer-synthesis.
        Phase A in /design mirrors ONLY the first 3 steps (no synthesis in design context):
          accessibility-specialist → animations-specialist → ui-designer ONCE (constraints baked in).

    - path: hive/references/specialist-triggers.md:1-173
      relevance: >
        Routing catalog. No dedicated design-time accessibility or animations trigger exists.
        The most relevant entries:
          ui:animations — trigger: ui:animations, placement: append, responds_with: {type: agent, id: animations-specialist}
            — this is the closest specialist-trigger for animations-specialist, but it's append placement (during execution),
            not pre-dispatch design-time.
          ui:major, ui:brand-redo — pre-exec triggers for full UI team phase (v1 deferred).
        KEY FINDING: specialist-triggers.md does NOT have a dedicated "design-time constraint-pass" trigger for
        accessibility-specialist + animations-specialist. The story AC says "routes through specialist-triggers" —
        this means the --include-constraints flag wiring follows the specialist-triggers routing PATTERN (catalog
        discipline) rather than routing through an existing named trigger entry. The developer will need to
        either register a new trigger or document the flag-based invocation as a named pattern in the catalog.
        This is a design decision for the developer, not a pre-wired entry today.

    - path: skills/hive/skills/design-dispatch/SKILL.md:1-218
      relevance: >
        d-2 router (already shipped). Phase 0 of /design invokes this skill.
        Invocation contract:
          Inputs: env (HIVE_SESSIONS_ENABLED, HIVE_PARALLEL_TEAMS, HIVE_TERMINAL_MUX, HIVE_DESIGN_MODE,
            CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS), parsed root hive.config.yaml, parsed consumer
            .pHive/hive.config.yaml or None, parsed graduation registry workflow list or None,
            workflow_name, epic_id (optional), arguments (--sequential flag + dep-depth summary),
            unblocked_stories[].
          Outputs: mode_decision enum (sessions|team|team-cmux|sequential|sandcastle|multica|cc-workflows),
            mode_reason (string), runner_path enum (hive-dag|orchestrator-narrated), runner_reason (string),
            field_sources map, field_sources.execution_runtime.epic_override (path or null), gate_violations[].
        env var: HIVE_DESIGN_MODE (not HIVE_EXECUTION_MODE — explicit per SKILL.md line 72).
        --sequential pass-through: consumed by Step 1 condition 5 (if --sequential present → sequential).
        Phase 0 of /design calls this skill, gets mode_decision, then routes to the appropriate mode atom.

    - path: .pHive/epics/substrate-coverage-and-test-cleanup/docs/design-discussion.md:§6
      relevance: >
        Q6 resolved text (Pattern B + toggle):
          "Pattern B + operator-toggle via --include-constraints accessibility,animations flag routing
          through specialist-triggers (composition is operator-directable; default-off keeps simple cases cheap)"
          User locked this at structured-outline gate.
        Q9 resolved text (wireframe handoff payload):
          "PNG + .f0 only, or include constraint doc from accessibility + animations?"
          Resolved by user: PNG + .f0 + bundled constraint doc. d-5 owns the payload contract.
        Q10 resolved text (design-mode-multica issue creation):
          "ui-designer alone, or one issue per persona (three issues per design call)?"
          Resolved: one issue per persona by default (mirrors execute-mode-multica precedent).

    - path: hive/agents/ui-designer.md:1-224
      relevance: >
        Persona dispatched ONCE in both default-off and toggle-on paths.
        Input expects: persona + brief (surface kind + topic + rendition count) + brand context + wireframe-protocol ref.
        When toggle ON: also receives accessibility constraint notes + animations constraint notes prepended to prompt.
        Output produces: .f0 wireframe files (via Frame0 CLI) + PNG exports + structured design brief (brief.md).
        Work Report format: Findings / Changes Made / Remaining Issues / Summary.
        Step files: step files in hive/workflows/steps/ui-design/ define HOW to execute.
        KEY: persona does NOT change — the constraint baking happens at the dispatcher level (prepend notes to prompt before dispatch), not inside the persona. Persona treats the constraints as part of its input context.

    - path: hive/agents/accessibility-specialist.md:1-96
      relevance: >
        Persona dispatched only when toggle ON (constraint pass before ui-designer).
        Input expects: story spec / component scope + any available artifacts for audit.
        For /design Phase A: receives the brief + brand context as the "design to audit for accessibility constraints".
        Output produces: Work Report with Findings (WCAG 2.1 AA violations or proactive concerns), Changes Made
          (when code is available), Remaining Issues, Summary.
        In Phase A context: produces ACCESSIBILITY CONSTRAINT NOTES — not a code remediation report (no code to fix yet).
        The notes surface: ARIA pattern guidance, keyboard navigation implications of proposed layouts, touch target
          sizing requirements, color contrast requirements from brand palette, semantic structure recommendations.
        Scope discipline: persona is code-level (WCAG remediation), but in Phase A it acts as a design-level advisor.
        Constraint notes format: Work Report shape, but Findings = proactive accessibility constraints for the brief.

    - path: hive/agents/animations-specialist.md:1-86
      relevance: >
        Persona dispatched only when toggle ON, after accessibility-specialist (depends_on: accessibility-critique pattern).
        Input expects: the brief + brand context + optionally the accessibility constraint notes (from accessibility pass).
        Output produces: Work Report with animation/motion constraints for the design brief.
        In Phase A context: produces ANIMATION CONSTRAINT NOTES — loading state recommendations, transition quality
          guidance, microinteraction suggestions, prefers-reduced-motion considerations.
        KEY: animations-specialist in Phase A reads the accessibility notes (if available) to avoid compounding
          accessibility issues — mirrors the design-review.workflow.yaml step 2 input contract.

    - path: hive/lib/package.json:1-14
      relevance: >
        Vitest runner registration. Current test script:
          "npx vitest run --reporter=verbose --root ../.. <list of test files>"
        skills/design/test/phase-a-toggle.test.mjs is NOT currently in the test script.
        FINDING: The developer must ADD skills/design/test/phase-a-toggle.test.mjs to the vitest test file list
          in hive/lib/package.json. The test file needs to be created (skills/design/test/ directory does not yet exist)
          AND registered in hive/lib/package.json's test script. This is a two-step action: create + register.

  PATTERNS_OBSERVED:
    - Pattern: Phase A insert positioning | File: skills/design/SKILL.md:55-64 | Detail: >
        The ONLY dispatch step today is step 3 (lines 55-64). Phase A goes ABOVE this step as a new named
        section "Phase A — Persona assembly" with its own phase header. Phase 0 (design-dispatch invocation)
        goes BEFORE step 1 (above line 39) or as a new "Phase 0" section at the top of the Process block.
        Net result: Process block becomes Phase 0 → (step 1 parse, step 2 brand) → Phase A → (original steps 3-7 shifted).
        Downstream steps 3-7 are UNCHANGED in content; only Phase A inserts ABOVE existing step 3.
        The "What /design is NOT" section (lines 135-140) states "Not a design-review. Critique is downstream —
        /design hands off to /design-review... It does not run the accessibility-specialist or animations-specialist passes itself."
        This line MUST be updated when Phase A ships — it becomes false when --include-constraints is on.

    - Pattern: Constraint baking via prompt concatenation | File: hive/agents/ui-designer.md + design-review.workflow.yaml | Detail: >
        The "constraints baked into prompt" mechanism is string concatenation: Phase A dispatcher prepends
        accessibility-constraint-notes block + animations-constraint-notes block to the ui-designer prompt.
        This is NOT a separate dispatch — ui-designer is called ONCE with the combined prompt (persona + brief +
        brand + constraints). The design-review.workflow.yaml pattern confirms: step 3 (design-critique) receives
        both critique outputs as optional inputs and the ui-designer synthesizes them. Phase A mirrors this but
        at prompt-assembly time rather than as separate step_file inputs.

    - Pattern: Specialist-triggers for Phase A | File: hive/references/specialist-triggers.md | Detail: >
        No existing trigger covers design-time constraint pass for accessibility + animations. The story says
        "routes through specialist-triggers" — the developer must either (a) register a new trigger
        (e.g., ui:design-constraints) following the catalog entry format, or (b) document the --include-constraints
        flag as routing to the specialist agents directly (outside the trigger catalog). Option (a) is more
        architecturally aligned with the catalog-first discipline. This is an open implementation decision
        for the developer step.

    - Pattern: Test file registration | File: hive/lib/package.json | Detail: >
        Existing pattern: test files are individually named in the vitest command in package.json test script.
        skills/design/test/phase-a-toggle.test.mjs must be ADDED to this list.
        The test file path does not yet exist — both the directory (skills/design/test/) and the file need creation.
        This differs from skills like design-dispatch which have test/ directories already.

  CONSTRAINTS:
    - Constraint: Phase A is a structural INSERT, not an extension of existing step 3 | Source: skills/design/SKILL.md:55-64 | Impact: >
        Step 3 (lines 55-64) is the ONLY dispatch step today. Phase A does not modify step 3 — it inserts
        a new phase ABOVE it. The original step 3 content (lines 55-64) becomes the default-off path
        inside Phase A: when --include-constraints is off, Phase A collapses to the existing step 3 behavior.

    - Constraint: "What /design is NOT" paragraph must be updated | Source: skills/design/SKILL.md:137 | Impact: >
        Line 137: "Not a design-review. Critique is downstream — /design hands off to /design-review via the
        index.yaml entry. It does not run the accessibility-specialist or animations-specialist passes itself."
        This is factually false when --include-constraints is on. The developer must update this paragraph
        to reflect the toggle behavior (default off → still true; toggle on → runs specialists as constraint pass only).

    - Constraint: ui-designer dispatched ONCE even when toggle ON | Source: story spec design_decisions | Impact: >
        Constraints baked into prompt before single dispatch. The dispatcher prepends notes, then calls ui-designer
        once. NOT: specialists → ui-designer-first-pass → ui-designer-second-pass. This is the critical
        architectural invariant that prevents the "two ui-designer calls" failure mode.

    - Constraint: d-5 owns handoff payload contract | Source: story spec AC Subsection 3 | Impact: >
        Phase A produces constraint notes as ARTIFACTS. d-1 AC Subsection 3 says bundling is "owned by d-5".
        d-1 only produces the notes + wires them into the ui-designer prompt. The actual payload SHAPE
        (how PNG + .f0 + constraint doc are bundled for downstream consumers) is d-5's contract.
        d-1 just produces the constraint doc as an artifact alongside the ui-designer outputs.

    - Constraint: design-dispatch (d-2) already shipped | Source: hive/lib/package.json test list | Impact: >
        skills/hive/skills/design-dispatch/SKILL.md exists. Phase 0 of /design calls this existing router.
        d-1 is not responsible for creating design-dispatch — only for inserting the Phase 0 invocation call
        in skills/design/SKILL.md.

    - Constraint: test file registration in package.json | Source: hive/lib/package.json:9 | Impact: >
        Developer must add skills/design/test/phase-a-toggle.test.mjs to the vitest command in
        hive/lib/package.json. Missing registration = test exists but npm test never runs it.

    - Constraint: 3 independent AC subsections | Source: hv-collab-review architect lock | Impact: >
        Each AC subsection (Phase A structural insert / Pattern B toggle / 3-persona handoff) must verify
        independently. Test file phase-a-toggle.test.mjs must have 3 describe blocks corresponding 1:1.
        No shared test state that makes one block depend on another.

  RISKS:
    - Severity: medium | Risk: specialist-triggers routing ambiguity | Evidence: >
        specialist-triggers.md has no design-time constraint trigger for accessibility + animations.
        Story says "routes through specialist-triggers" but the mechanism is not pre-wired.
        Developer must either register a new trigger or document the direct dispatch pattern.
        If the developer adds a new trigger, that's a file modification to specialist-triggers.md
        which is not currently in d-1's files_to_modify list.

    - Severity: medium | Risk: "What /design is NOT" paragraph false on toggle-on | Evidence: skills/design/SKILL.md:137 | Impact: >
        The existing text will be inaccurate when --include-constraints is wired. Architect reviewer
        should flag if developer doesn't update this paragraph.

    - Severity: low | Risk: test directory and package.json registration are two separate steps | Evidence: hive/lib/package.json:9 | Impact: >
        If developer creates the test file but forgets package.json registration, npm test won't catch
        Phase A regressions. The metric.source.ref in the story spec calls vitest explicitly, so
        developer should be aware.

  UTILITIES_AVAILABLE:
    - Utility: design-dispatch SKILL.md | File: skills/hive/skills/design-dispatch/SKILL.md | Relevance: >
        Phase 0 invocation target. Already shipped (d-2). Invocation contract documented in SKILL.md.
    - Utility: design-review.workflow.yaml | File: hive/workflows/design-review.workflow.yaml:8-81 | Relevance: >
        Orchestration template anchor for Phase A serial dispatch shape. Developer mirrors this pattern.

  UNANSWERED_QUESTIONS:
    - Whether "routes through specialist-triggers" means a new catalog entry must be added to specialist-triggers.md
      (which would require adding that file to files_to_modify) or whether it means following the trigger
      invocation PATTERN without a new catalog entry. Story spec files_to_modify does not list specialist-triggers.md.
    - Whether skills/design/test/ directory needs any scaffolding file (e.g., package.json) or if the root
      hive/lib/package.json vitest invocation is sufficient (current pattern suggests the latter).

  INCONSISTENCY_RISK_SIGNALS:
    - Signal: "routes through specialist-triggers" with no named trigger | Where: story spec AC Subsection 2
      | Detail: specialist-triggers.md has no design-time constraint trigger; developer may interpret this
      as adding a new trigger (expanding files_to_modify) or as direct dispatch. Story spec files_to_modify
      does not include specialist-triggers.md. Risk of implementation drift.

    - Signal: "What /design is NOT" paragraph (line 137) contradicts toggle-on behavior | Where: skills/design/SKILL.md:137
      | Detail: existing text says /design does not run accessibility-specialist or animations-specialist.
      Toggle-on makes this false. Must update — not listed explicitly in story spec cross-cutting.

VALIDATION NOTE:
  Checked: vitest (test runner), specialist-triggers catalog pattern
  Source: codebase-only (no external library lookup needed — all components are internal to hive repo)
  Confidence: high
  Findings: >
    vitest confirmed as runner via hive/lib/package.json. No version pin change needed.
    All personas (accessibility-specialist, animations-specialist, ui-designer) confirmed in hive/agents/.
    design-dispatch already shipped (d-2). design-review.workflow.yaml:8-81 confirmed as 4-step template anchor.
    No external API dependencies introduced by d-1.
