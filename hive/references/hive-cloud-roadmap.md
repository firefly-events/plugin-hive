# Hive Cloud Roadmap

S16 stub to close the forward reference from `hive/references/session-system-prompt-spec.md`.

- Status: placeholder only; deferred A8 bootstrap remains out of scope for this epic slice.
- Source escalation: `.pHive/cycle-state/cwc-2026-integration.yaml` → `architecture:stub-path`.
- Deferred A8 topics: agent creation bootstrap and environment registration bootstrap.
- This slice preserves the reference graph only; it does not create `agent_id` or `environment_id` runtime flows.
- Context docs:
- `.pHive/epics/cwc-2026-integration/docs/design-discussion.md`
- `.pHive/epics/cwc-2026-integration/docs/structured-outline.md`
- Cloud-mode rollout details should land in the separate Hive Cloud epic.
- Until then, `execution.substrate: messages` remains the default local path.

