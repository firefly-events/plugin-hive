{
  "name": "@hive/multica-adapter",
  "version": "0.1.0",
  "description": "Multica task-tracking adapter for Hive ABI 1.0.0",
  "bin": {
    "multica-adapter": "./index.ts"
  },
  "type": "module",
  "scripts": {
    "test": "node --test --import tsx 'test/**/*.test.ts'"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "tsx": "^4.22.3"
  }
}
